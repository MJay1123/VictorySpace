<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.victoryspace.vics.comment.query.mapper.CommentQueryMapper">
    <resultMap id="CommentResultMap"
               type="com.victoryspace.vics.comment.query.dto.CommentQueryDTO">
        <result property="id" column="id"/>
        <result property="voteId" column="vote_id"/>
        <result property="memberId" column="member_id"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <select id="findAll" resultMap="CommentResultMap">
        SELECT id,
               vote_id,
               member_id,
               content,
               created_at,
               updated_at,
               deleted_at
        FROM comment
    </select>

    <select id="findById" resultMap="CommentResultMap">
        SELECT id,
               vote_id,
               member_id,
               content,
               created_at,
               updated_at,
               deleted_at
        FROM comment
        WHERE id = #{id}
    </select>

    <select id="findByVoteId" resultMap="CommentResultMap">
        SELECT id,
               vote_id,
               member_id,
               content,
               created_at,
               updated_at,
               deleted_at
        FROM comment
        WHERE vote_id = #{voteId}
    </select>

    <select id="findByMemberId" resultMap="CommentResultMap">
        SELECT id,
               vote_id,
               member_id,
               content,
               created_at,
               updated_at,
               deleted_at
        FROM comment
        WHERE member_id = #{memberId}
    </select>
</mapper>